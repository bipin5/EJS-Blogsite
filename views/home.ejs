<%- include('partials/header'); -%>

<h1>Home</h1>
<p> <%= StartingContent.substring(0,50) %> </p>

<% posts.forEach(function(post){  %>
  <h1> <%= post.title.substring(0,50) %> </h1>
  <p> <%= post.message.substring(0,50) + " ..." %> <a href="/posts/<%= post.title %>">Read More</a> </p>
<% }); %>


<%- include('partials/footer'); -%>
